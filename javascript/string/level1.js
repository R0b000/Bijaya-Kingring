const user = 'ram thapa'
//write your code here

//We have a property called toUpperCase so use it but we have one problem since its the const we cannot change the 
//values so there is either a way to create a new const or let or var or add toUpperCase() at the end of the console.
const result = user.toUpperCase()
console.log("This result is done by creating a new const result:", result)

console.log("This result is done by adding a toUpperCase() at end of the user in console:", user.toUpperCase())
//RAM THAPA


